export enum TransactionStatusEnum {
  pending = "В ожидании",
  completed = "Выполнен",
  failed = "Отклонен",
}

export enum TransactionStatusServiceEnum {
  PENDING = "pending",
  COMPLETED = "completed",
  FAILED = "failed",
}
